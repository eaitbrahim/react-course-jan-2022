let numArray = [1, 2, 3, 4, 5, 6];

// Using indexOf returns the index of the searched value , -1 if it doesn't exist...
console.log(numArray.indexOf(7));

// Using includes returns true of false
console.log(numArray.includes(5));